services:
  db:
    ports:
      - "5432:5432"

  backend:
    environment:
      NODE_ENV: development
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    depends_on:
      - backend
    ports:
      - "5173:5173"